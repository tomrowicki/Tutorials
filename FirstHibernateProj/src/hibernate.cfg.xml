<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

  <session-factory>
    <!-- Database connection settings -->
    <property name="connection.driver_class">org.postgresql.Driver</property>
    <property name="connection.url">jdbc:postgresql://localhost:5432/hibernatedb</property>
    <property name="connection.username">test</property>
    <property name="connection.password">test</property>

    <!-- JDBC connection pool (use the built-in) -->
    <property name="connection.pool_size">1</property>

    <!-- SQL dialect -->
    <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

    <!-- Enable Hibernate's automatic session context management -->
   <!-- <property name="current_session_context_class">thread</property> -->

    <!-- Disable the second-level cache  -->
    <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

    <!-- Echo all executed SQL to stdout -->
    <property name="show_sql">true</property>

    <!-- Drop and re-create the database schema on startup -->
    <property name="hbm2ddl.auto">update</property> <!-- saving new object does not overwrite existent data if set to "update", "create" does -->  
    
   <!-- <mapping resource="org/hibernate/tutorial/domain/Event.hbm.xml"/> -->
   
   <!-- Names the annotated entity class -->
<!--    <mapping class="org.javabrains.koushik.dto.UserDetails"/> -->
<!--    <mapping class="org.javabrains.koushik.dto.Vehicle"/> -->
				<!-- inheritance -->
<!-- 	<mapping class="org.javabrains.koushik.inheritance.Vehicle"/> -->
<!-- 	<mapping class="org.javabrains.koushik.inheritance.TwoWheeler"/> -->
<!-- 	<mapping class="org.javabrains.koushik.inheritance.FourWheeler"/> -->
				<!-- CRUD    -->
	<mapping class="org.koushik.criteria.UserDetails"/>
  </session-factory>
</hibernate-configuration>
